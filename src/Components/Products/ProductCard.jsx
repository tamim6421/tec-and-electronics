
const ProductCard = ({product}) => {
    const{_id, name, bName, photo} = product
    console.log(product)
    return (
        <div>
            <div>
   
            </div>
        </div>
    );
};

export default ProductCard;